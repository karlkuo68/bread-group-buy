# bread-group-buy v001-checkpoint-2026-05-06

## 此版本是什麼狀態
- **背景**：原本此專案沒有 `.backups/` 目錄，僅靠根目錄的 `index.html.bak.*` 檔案做版本保留。
- **觸發補建**：2026-05-06 備份盤點時發現缺口 — 最新 `.bak` 檔停在 `20260429-1103-stable`，但工作檔 `index.html` 已被改動到 2026-05-05 03:46（mtime），中間有 5 天的變動沒有任何備份。
- **此資料夾內容**：對應 2026-05-05 03:46 的 `index.html`（269,516 bytes）+ 同步用的 `push.command`。

## 對應根目錄狀態
- `index.html` mtime：2026-05-05 03:46（GitHub HEAD 同步後狀態）
- `push.command` mtime：2026-05-05 03:47

## 後續建議
- 之後改動前，請改用 `.backups/v002/`、`v003/`... 遞增建立，不要再用根目錄 `.bak.*`（兩種混用會混亂）。
- 舊的 5 個 `.bak.*` 檔可保留作為歷史記錄，超過一個月再清。
